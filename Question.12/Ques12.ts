// // Question no 12 : Greetings
let friendName:string[] = ["Laiba", "Ginan", "Jannat"];
for (let idx = 0; idx < friendName.length; idx++) {
  console.log(`Hello ${friendName[idx]} , I hope you are doing well `);
}